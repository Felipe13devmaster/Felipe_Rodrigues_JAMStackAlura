import React from 'react';
import styled from 'styled-components';

const ContainerProjetos = styled.div`
  
`;

const WrapperProjetos = ({ children }) => (
  <ContainerProjetos>{children}</ContainerProjetos>
);

export default WrapperProjetos;
